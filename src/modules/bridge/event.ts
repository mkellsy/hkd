enum BridgeEvent {
    AccessoryChange = "accessory_change",
    PublishBridge = "publish_bridge",
    Listening = "listening",
    Shutdown = "shutdown",
    RestartHub = "restart_hub",
    RestartBridge = "restart_bridge",
}

export default BridgeEvent;
